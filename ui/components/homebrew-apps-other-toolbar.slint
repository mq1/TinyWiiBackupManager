// SPDX-FileCopyrightText: 2026 Manuel Quarneti <mq1@ik.me>
// SPDX-License-Identifier: GPL-3.0-only

import { HomebrewAppsFilter } from "homebrew-apps-filter.slint";
import { MirroredProperties } from "../mirrored-properties.slint";

export component HomebrewAppsOtherToolbar inherits HorizontalLayout {
    alignment: space-between;
    height: 38px;
    padding-right: 10px;

    HomebrewAppsFilter { }

    Text {
        height: parent.height;
        vertical-alignment: center;
        horizontal-alignment: end;
        text: "\{MirroredProperties.homebrew-apps.length} app\{MirroredProperties.homebrew-apps.length == 1 ? "" : "s"}   ~ \{MirroredProperties.drive-info.apps-mib.to-fixed(2)} MiB";
    }
}
