// SPDX-FileCopyrightText: 2026 Manuel Quarneti <mq1@ik.me>
// SPDX-License-Identifier: GPL-3.0-only

import { OscFilter } from "osc-filter.slint";
import { UiState } from "../ui-state.slint";
import { Link } from "link.slint";

export component OscOtherToolbar inherits HorizontalLayout {
    alignment: space-between;
    height: 38px;
    padding-right: 10px;

    OscFilter { }

    VerticalLayout {
        height: parent.height;
        alignment: center;

        HorizontalLayout {
            spacing: 20px;

            Text {
                text: "refreshed \{UiState.osc-refreshed-x-hours-ago}h \{UiState.osc-refreshed-x-minutes-ago}m ago";
            }
        }
    }
}
