// SPDX-FileCopyrightText: 2026 Manuel Quarneti <mq1@ik.me>
// SPDX-License-Identifier: GPL-3.0-only

import { Card } from "card.slint";
import { LineEdit, HorizontalBox, VerticalBox } from "std-widgets.slint";
import { MyPalette } from "../styling.slint";
import { IconSet, IconDisplay } from "@lucide";
import { State } from "../state.slint";
import { MyLineEdit } from "my-line-edit.slint";

export component GameFilter inherits Card {
    width: 200px;
    height: 38px;

    HorizontalLayout {
        VerticalBox {
            alignment: center;

            IconDisplay {
                icon: IconSet.Search;
                size: 14px;
                stroke: MyPalette.foreground;
            }
        }

        VerticalLayout {
            alignment: center;

            MyLineEdit {
                text <=> State.game-filter;
                placeholder-text: "Search by Title/ID";
                changed text => {
                    State.filter-games();
                }
            }
        }
    }
}
