// SPDX-FileCopyrightText: 2026 Manuel Quarneti <mq1@ik.me>
// SPDX-License-Identifier: GPL-3.0-only

import { Config } from "types.slint";

export global Rust {
    pure callback open(string) -> string;
    pure callback pick-mount-point() -> string;
    pure callback write-config(Config) -> string;
    pure callback get-drive-usage(string) -> {a-used: float, b-total: float};
}
